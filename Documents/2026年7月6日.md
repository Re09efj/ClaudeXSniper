# 2026年7月6日 作業ログ

## 1. GAPBSクラッシュの根本原因特定と修正

前日までの調査を継続。GAPBS(BFS/PR)がSID上でsendInstruction系SIGSEGV(signal 11)で
クラッシュする問題を深追い。

**根本原因**: `sift/recorder/recorder_base.cc`の`sendInstruction()`内、`dyn_addresses`への
生ポインタタッチが、Pinの`PIN_AddInternalExceptionHandler`(内部例外ハンドラ)にキャッチされ、
POSIXシグナルハンドラには一切届かない設計だったため、当初実装した signal+sigsetjmp ベースの
タッチガードは最初から機能していなかったと判明。

**修正**: 生ポインタの直接タッチを、Pin公式の安全メモリアクセスAPI`PIN_SafeCopy()`
(無効アドレスなら0バイトコピーでクラッシュせず返る)に置き換え。BFS/PR×2TH/8THの
スモークテストで全コア正常な命令数(非ゼロ)を確認し、`docker.io/snipersim/snipersim:latest`
に反映(旧ロック混入版は`:pre-wake-fix`→さらに後で`:with-broken-lock-fix`等のタグで保存)。

途中、「Sift::Writerのクローズ処理を排他ロックで囲む」という別の修正案も試したが、
これはPageRank(PR)の記録を壊す(Core0のみ記録、他コア0命令)副作用が判明し破棄。
最終的に採用したのはPIN_SafeCopyのみのシンプルな修正。

「8THは7/8スレッド完了ってどういうことやねん」というユーザーの指摘を受け、中途半端な
「ほぼ解決」という報告を撤回し、BFS/PR×8/12/16THでのデータ完全性検証を継続。最終的に
SafeCopy修正版でBFS 8THの全8コアが2.6M〜11M命令という妥当な分布で記録されることを確認。

## 2. 深夜の自動実行と朝の状況

深夜、Outputsフォルダのバックアップ・GAPBS全廃止・通信行列取得・AKARIN/MPOマッピング
生成・SID/Purple振り分け実行という一連の作業を自律的に実施(ユーザーは就寝、TOKENACHを
シャットダウン。SIDは別マシンの常時稼働サーバのため無関係と確認済み)。

朝の確認で、`run_tonight.py`(heavy=NPB→SID、light=その他→Purple)が794ジョブ中51件しか
進んでおらず(3.5時間で)極端に遅いことが判明。原因調査へ。

## 3. スケジューリング崩壊の根本原因: 新規ワークロードの実消費コア未測定

`_WIDTH_BASELINE_2TH`にcanneal/dedup/x264/WATER_NSQUARED/GUPSのエントリが無く、
`host_width_pct()`のデフォルト値100%にフォールバックしていたことが判明。実測すると
canneal≈95%、dedup≈129%、x264≈93%、WATER_NSQUARED≈121%、GUPS≈147%と、最大47%の
過小評価があり、容量制約付きスケジューラが許容量を超えてジョブを詰め込みすぎ、
load average 55超えという事故につながったと特定。

`run_tonight.py`とコンテナ群を停止し、5ワークロード×[8,12,16]THの実使用コア計測
バッチ(`batch_measure.sh`)を開始。

### batch_measure.shの重大バグ発見
各イテレーション末尾の`podman ps | xargs podman kill`が、その時点で動いている**全**
podmanコンテナを無差別killしていることが判明。並列実行中の別ジョブ(x264_16TH、
GUPS_16TH、後にはfluidanimateの検証コンテナも)を巻き添えでkillし、捏造データを
記録していた。コンテナIDをマウントパスから特定して個別killする安全版
(`parallel_one_workload.sh`/`measure_single.sh`)に切り替えて再測定し、影響を受けた
x264_16TH/GUPS_16THを正しく再計測した。

## 4. ハイブリッドスケジューラの実装

「事前推定はそのまま使い、投入直前に現在の実測負荷を見て、上限を超えるなら投入を
遅らせる」というユーザー提案を実装。`ultra_orchestrator.py`の`_CapacityPool`に
`hard_limit`/`live_load_fn`を追加し、`live_sid_load_cores()`(podman stats合計)/
`live_purple_load_cores()`(SSH経由でps -eo pcpu合計)で実測。TOCTOU回避のため、
実測待ち時間中はロックを解放してから再取得する設計。SID=24コア、Purple=56コアを
ハード上限として設定。単体テストで、高負荷中は待機し負荷低下後に通過することを確認。

## 5. WATER_NSQUAREDの深追い: Sniper本体のfutexデッドロック発見

WATER_NSQUARED(SPLASH2)が8THで確率的にハングする問題を深追い。gdbでアタッチしたところ、
**全8スレッドが完全に同一の命令アドレスで停止**していることを確認(単発の遅延ではなく
真性の相互デッドロック)。6並列実行で2/6がハング、単独実行では6/6成功という
タイミング依存の再現パターン。

Sniper本体のソース(`common/system/syscall_server.cc`)を調査し、`FUTEX_CMP_REQUEUE`
実装に開発者自身の`"race condition may have occured"`というコメントを発見。各コアが
独立したホストスレッドでトレースを再生する設計上、実行時の本当のhappens-before関係を
SIFTトレース形式が保持していないため、記録時と異なる順序でFUTEX_WAIT/WAKEが処理され、
永遠に来ないWAKEを待つスレッドが生まれうると根本原因を特定。

`TraceManager::signalDone()`に「1スレッドだけ残ったら強制resumeThread()する」修正を
追加し本番`:latest`に反映(fluidanimateで実際に発生していた「最後の1スレッドが
siblingの終了を待ち続ける」パターンには有効)。しかしWATER_NSQUARED/BFSで見られる
「全スレッド同時凍結」パターンには無効(誰もsignalDone()を呼ばないため)。真の修正には
シミュレータのコア部分(トレースリプレイの順序保証)の再設計が必要と判断し、深追いを断念。

WATER_NSQUARED、および後に同じ症状を再現したBFS/PR(GAPBS)を`WORKLOADS`から除外。

## 6. fluidanimate/lavaMDの再評価

削除済みだったfluidanimate/lavaMDを、現在の修正版イメージで再検証:
- **fluidanimate**: 両スレッドとも`TRACE DONE`後にsignal 11。当初「GAPBSと同じ無害な
  クラッシュ」と判断したが、実際は**Sniperシミュレータ側がハングする**(アプリ側は
  正常終了に近いところまで進むが、シミュレータのタイミング処理側が停止)別バグと判明。
  上記5節の`signalDone()`修正で解消し、`[SNIPER] End`まで到達するようになったが、
  犠牲になった1スレッドのデータが大幅欠損(241M命令 vs 2572命令)する副作用が残る
- **lavaMD**: SIDでは即座にPin injectorごと失敗(`assertion failed: !WIFEXITED(status)`)。
  Purple(Jin本人の`/home/jin/sniper-detloc-backup`+Agungさんの古いPin 3.7)で試したところ
  `[SNIPER] End`まで到達(14.22秒)。データ完全性は未検証(sim.out未生成、1スレッド分の
  完了ログ欠落)だが、SIDの即死状態より大きく前進

いずれも今回は深追いせず不採用のまま。粒子シミュレーション系統
(fluidanimate/lavaMD/water_nsquared)は結果的に全滅。

## 7. 最終ワークロード構成の確定(8種)

一日を通した紆余曲折の末、`BT, FT, IS, MG, canneal, dedup, x264, GUPS`の8種に確定。
BFS/PRは一度SafeCopy修正の成功を確認して再追加したが、直後の問題サイズ較正実測中に
BFSでも同じfutexデッドロックを再現し、再度除外。

### canneal/dedup/x264/GUPSはbench_classを見ない(発見)
`get_binary_args()`がbench_classパラメータを実質見ておらず、class SとWで完全に
同じ入力・同じ実行になっていた。S/W両方を回すのは無価値な重複実行だったため、
これら4ワークロードは`bench_class="W"`一本のみ実行する方針に変更。

`utility/run_profile.py`の`_DEFAULT_STEP`(クラス外挿倍率)にもこの4ワークロードの
値=1.0を追加。元は未登録で汎用フォールバック30倍が適用され、`canneal_W`の推定壁時計
時間が実際の30倍(10184秒→305526秒)という完全な虚偽値になっていたことも同時に修正。

## 8. SID/Purple振り分け: スイート単位から実行時間ベースへ

当初「NPB(重量級)→SID、PARSEC/GUPS(軽量級)→Purple」というスイート単位の区分だったが、
ユーザーから「重いかどうかはスイートではなく実行時間ベースで決めるべきでは」との指摘。
実測すると:

```
canneal(10184s) > BT(10036s) > x264(7765s) > dedup(3506s) > MG(1715s)
> GUPS(1534s) > FT(829s) > IS(401s)
```

cannealがBTより重く、MG/FT/ISはcanneal/x264より軽いことが判明し、スイート単位の
区分は実態と合わないと結論。全8ワークロードを実測壁時計時間の重い順に並べ、
機械的に上位3つ(canneal/BT/x264)をSID、残り5つ(dedup/MG/GUPS/FT/IS)をPurpleに
割り当てる方式に変更(`run_tonight.py`)。MG/FT/ISがPurpleで実行されるのはこれが初めて
(参照データなし、未検証の実行パス)。

## 9. DeLoc通信行列の確認

最終8ワークロード×W級×[2,8,12,16]THの全32通りが`Data/comm_matrices/`に既に
揃っていることを確認(追加取得不要)。

## 10. 本実行の開始

`run_tonight.py`を最終形に更新し19:41に起動(nohup、`run_tonight_20260706_194150.log`):
- SID: canneal, BT, x264(NPB系はS+W、canneal/x264はWのみ)
- Purple: dedup, MG, GUPS, FT, IS(dedup/GUPSはWのみ、MG/FT/ISはS+W)
- 戦略: Packed/Scatter/HPO/EPO/MPO/akarin_h/akarin_l
- 見積もり(逐次換算): SID 173.8時間、Purple 139.1時間、総ジョブ数355件
- Purple側の結果は`sniper_sim_purple.py`が実行後に自動でrsyncしSID側`Outputs/`へ回収
  (既存の仕組みをそのまま利用、追加対応なし)

19:45時点でスケジューリング開始・ジョブ投入が正常に進行していることを確認。
